apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: envvault-production
spec:
  selector:
    app: auth-service
  ports:
    - port: 8001
      targetPort: 8001
      protocol: TCP
  type: ClusterIP
